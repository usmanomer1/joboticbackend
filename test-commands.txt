# Test Commands (copy these one by one)

# 1. Health Check (should work)
curl https://jobotic-backend.vercel.app/api/health

# 2. Job Search (should work)
curl -X POST https://jobotic-backend.vercel.app/api/jobs/search \
  -H "Content-Type: application/json" \
  -d '{"jobTitle": "software engineer", "location": "San Francisco, CA"}'

# 3. AI Job Matching (should work - you said this worked)
curl -X POST https://jobotic-backend.vercel.app/api/jobs/match \
  -H "Content-Type: application/json" \
  -d @test-job-match.json

# 4. Resume Analysis (may fail due to Gemini API)
curl -X POST https://jobotic-backend.vercel.app/api/resume/analyze \
  -H "Content-Type: application/json" \
  -d @test-resume-analyze.json

# 5. Resume Optimization (may fail due to Gemini API)
curl -X POST https://jobotic-backend.vercel.app/api/resume/optimize \
  -H "Content-Type: application/json" \
  -d @test-resume-optimize.json

# 6. PDF Generation (CORRECTED ROUTE)
curl -X POST https://jobotic-backend.vercel.app/api/download/resume/download \
  -H "Content-Type: application/json" \
  -d @test-pdf-download.json